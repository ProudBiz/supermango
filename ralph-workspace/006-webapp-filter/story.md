# 웹앱 필터링

## Description
사용자가 태그 검색, 채널 필터, 워크스페이스 필터로 피드를 좁혀서 볼 수 있다.

## Acceptance Criteria
- 워크스페이스 드롭다운으로 특정 워크스페이스만 필터링할 수 있다
- 채널 드롭다운으로 모든 채널 또는 특정 채널만 필터링할 수 있다
- 태그 검색 입력으로 특정 태그가 포함된 메시지만 필터링할 수 있다
- 필터 조합이 가능하다 (워크스페이스 + 채널 + 태그)
- 필터 적용 시 피드가 실시간으로 갱신된다
